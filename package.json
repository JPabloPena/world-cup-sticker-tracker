{
  "name": "world-cup-sticker-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "build": "pnpm --filter client run build && pnpm start",
    "start": "node server/index.js",
    "seed": "node server/database/seed.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "recharts": "^3.8.1",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
